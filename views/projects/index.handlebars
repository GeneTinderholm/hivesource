<div class='container'>
  <div class='row'>
    {{#each projects as |project|}}
      <div class="col-md-3">
        <a href="/projects/{{project.id}}">{{project.name}}</a>
      </div>
    {{/each}}
  </div>
</div>
